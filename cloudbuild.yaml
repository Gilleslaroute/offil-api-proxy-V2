options:
  default_logs_bucket_behavior: REGIONAL_USER_OWNED_BUCKET

steps:
  # --- BUILD DOCKER IMAGE ---
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'europe-west9-docker.pkg.dev/$PROJECT_ID/offil-api/offil-rome-proxy', '.']

  # --- PUSH IMAGE ---
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'europe-west9-docker.pkg.dev/$PROJECT_ID/offil-api/offil-rome-proxy']

  # --- DEPLOY TO CLOUD RUN ---
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      [
        'run', 'deploy', 'offil-rome-proxy',
        '--image', 'europe-west9-docker.pkg.dev/$PROJECT_ID/offil-api/offil-rome-proxy',
        '--platform', 'managed',
        '--region', 'europe-west9',
        '--allow-unauthenticated',
        '--port', '8080'
      ]

images:
  - 'europe-west9-docker.pkg.dev/$PROJECT_ID/offil-api/offil-rome-proxy'
  
